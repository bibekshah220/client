import NavBar from "../components/header";

const LandingPage = () => {
  return (
    <main className="px-24">
      {/* nav bar */}
      <NavBar />
      {/* page content */}
      {/* footer */}
    </main>
  );
};

export default LandingPage;
